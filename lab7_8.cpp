#include <iostream>

using namespace std;

int main() {
    int counter = 0; // Встановлення початкового значення

    while (counter < 10) {
        cout << "counter: " << counter << endl;
        counter++; // Збільшення значення counter
    }

    return 0;
}
